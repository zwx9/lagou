$(function () {
    $("#loginForm").validate({
        rules: {email: {required: !0, email: !0}, password: {required: !0}},
        messages: {email: {required: "请输入登录邮箱地址", email: "请输入有效的邮箱地址，如：vivi@lagou.com"}, password: {required: "请输入密码"}},
        submitHandler: function (a) {
            var b, c;
            $("#remember").prop("checked") ? $("#remember").val(1) : $("#remember").val(null), b = $("#email").val(), c = $("#password").val(),
                //$(a).find(":submit").attr("disabled", !0),
                $.ajax({
                type: "POST",
                data: {email: b, password: c},
                url: "http://localhost:8080/userServlet/login",
                    dataType: "text",
            success:function(result){{
                if (result){
                    top.location.reload();

                    window.location.href("/index.jsp");
                } else{
                    $("#beError").text(b.msg).show();
                }
            })
        }
    }), $("body").on("click", "a.btn_s", function () {
        $.colorbox.close(), parent.jQuery.colorbox.close()
    }), $(".inline").colorbox({inline: !0}), $(".errorTips").click(function () {
        errorTips("上传附件格式错误!")
    }), $("#resend").click(function () {
        var a = $("#userid").val(), b = $("#resend_type").val();
        $.ajax({type: "POST", data: {code: a, type: b}, url: ctx + "/user/resendActivatedMail"}).done(function (a) {
            a.success ? $.colorbox({inline: !0, href: "#resend_success", title: "验证邮件发送成功"}) : alert(a.msg)
        })
    })
});